# Pre-Launch Readiness — Real-World Testing Checklist

**Date**: 2026-06-01 (updated)
**Generated by**: pre-launch fix wave A (commit `4c57685`) + UI deep-dive + fix wave B (this update)
**Scope**: Everything mechanical that can be done before real-world testing on the three target machines (Creality K2 Plus, Laguna Swift 5x10, Makera Carvera 3-axis and 4-axis).

This doc is the operator's "is the app ready?" checklist. It lists what's been verified, what runbooks exist, what's still deferred, and what the operator needs to bring to each first real run.

---

## TL;DR — ALL DEFERRED ITEMS CLOSED

**The app is at pro-app UX parity for the three target machines, has a full CAD Design workspace (Part / Sketcher / Assembly / Drawing views), has a wired global E-stop, all 5 big-design UX moves from the deep-dive have landed, the critical vitest CVE is closed, and every deferred item from the session is shipped.**

Quality gates at the time of this doc:
- `npm test` → **14,189 passed / 0 failed / 2 skipped** — Δ +159 from prior baseline; **Δ +578** since the session start (13,611)
- `npm run typecheck` → clean
- `npm run test:python` → **80 passed / 26 skipped** — Δ +46 cases this wave (all `@requires_cadquery` / `@requires_planegcs`-gated; will run anywhere those deps are installed)
- `gcode-safety` skill verdict on prior G-code change sets: **SAFE for all three target machines**
- 0 broken-path findings remaining
- Vitest 3.x → **4.1.8+** upgrade landed: **CVE GHSA-5xrq-8626-4rwp CLOSED** (CVSS 9.8 arbitrary file read/execute)
- **E-stop wired** end-to-end: AppHeader red button → native confirm → IPC dispatch → K2 Moonraker `/printer/emergency_stop` (canonical Klipper M112), Carvera + Laguna toast advisories pointing at physical e-stop
- **CAD face selection** wired: `cad.tessellate_with_ids` returns per-triangle face IDs; Viewport3D raycasts hit triangles → CadQuery face entity → LineSegments overlay highlight + status badge
- **CAD 2D Sketcher with planegcs solver** wired: 5 entity types + 5 constraint types, auto-solve, structured error envelope
- **CAD Assemblies via cq.Assembly** + per-child 4×4 transforms; STEP/STL export
- **CAD 2D Drawings** (Front/Top/Right/Iso projections via OCCT hidden-line-removal); SVG inline render + PDF/SVG export
- **DesignWorkspace TabBar** switches between Part / Assembly / Drawing views
- **`ipc-cad.ts` channel pin: 10 documented CAD channels** (cad:execute, cad:export, cad:listOperations, cad:tessellateWithIds, cad:solveSketch, cad:createAssembly, cad:tessellateAssembly, cad:exportAssembly, cad:projectDrawing, cad:exportDrawing)

**There are no deferred items remaining from the session's deep-dive synthesis.**

---

## What was done in this pre-launch wave

### Machine profile correctness (G-code-touching)
| Machine | Field | Old | New | Why |
| --- | --- | --- | --- | --- |
| Laguna Swift 5x10 | `minSpindleRpm` | 8000 | 6000 | CLAUDE.md spec: 3HP/6HP at 6,000–24,000 RPM. Old floor blocked valid slow plywood cuts. |
| Laguna Swift 5x10 | `maxSpindleRpm` | 18000 | 24000 | Same. Old ceiling blocked valid high-speed light cuts. |
| Makera Carvera 3-axis | `minSpindleRpm` | 6000 | 13000 | CLAUDE.md spec: 200 W spindle rated 13,000–15,000 RPM. Sub-13k risks spindle damage. |

`clampSpindleRpm()` (`src/main/post-process.ts:185-202`) emits an operator-visible warning on both edges, so any clamp surfaces in the run log.

### Speculative post deletion (My-Shop-Only enforcement)
- `resources/posts/cnc_5axis_fanuc.hbs` — DELETED
- `resources/posts/cnc_5axis_siemens.hbs` — DELETED

CPS dialect routing (`src/main/machine-cps-import.ts`) now falls back to `cnc_generic_mm.hbs` for `fanuc` / `siemens` dialect imports. The dialect enum is preserved so existing saved profiles still load (Safety Rule 2). The generic post emits explicit G21/G90/G17/M30 with a multi-line `UNVERIFIED` warning header — safe on any 3-axis Fanuc/Siemens control.

### UI coverage (manufacture workspace)
~25 BEM classes that were referenced by recently-added components had zero matching CSS rules. The dashboard fix in Cycle 233 closed the Workshop side; this wave closed:

- `CalibrationPanel` — 8 K2 calibration cards (tower test, flow rate, pressure advance, retraction, MVF, tolerance, SCV, VFA) now styled
- `ManufactureAuxPanels` — `.k2-send-section`, `.manufacture-cam-stale-banner`, `.util-laguna-*`, `.manufacture-carvera-row`, `.util-tool-table`
- `LagunaNestingPanel` — `.laguna-nesting-panel` / `__result` / `__list`
- `PlateTabs` — `.plate-tab-wrap--active` (active plate now visually distinct)
- `SettingsView` — `.settings-probe-temps` / `__row` for Moonraker probe display
- New shared `.row` primitive in `layout.css`

### Documentation (first-time-user readiness)
| Doc | Status | Scope |
| --- | --- | --- |
| `docs/SMOKE-K2-MOONRAKER.md` | EXTENDED | Now has explicit Moonraker URL discovery (DHCP table / touchscreen / mDNS / port 7125 / both URL forms) |
| `docs/SMOKE-LAGUNA-RICHAUTO.md` | **NEW** | USB transfer to RichAuto A-series pendant, accepted formats, sub-directory limits, 6-zone vacuum, dust collection, RPM ramp, e-stop |
| `docs/SMOKE-CARVERA-CLI.md` | **NEW** | pip install + virtualenv, `--version` verify, configure `carveraCliPath` in Settings, send a test job, troubleshooting (ENOENT / permissions) |
| `docs/CAM_4TH_AXIS_REFERENCE.md` | EXTENDED | New Part 1.5: rotary mount, chuck install, tailstock, X-offset measurement, Y=0 enforcement, pre-cut sanity table |
| `README.md` | EXTENDED | Quick Start now includes `pwsh ./scripts/bundle-orca-slicer.ps1` step + Next Steps section linking all SMOKE-* docs |

### Settings UI additions
- `External tool paths` section in `SettingsView.tsx` — exposes `carveraCliPath` and `carveraCliExtraArgsJson` (schema fields existed but were UI-invisible)
- Moonraker URL field gained a hint text (k2plus.local / port 7125 / DHCP discovery)

### Regression protection
- New `src/main/machine-profile-spindle-pin.test.ts` — 3 it() blocks reading machine JSONs schema-independently so a future regression to the old wrong RPM values is caught immediately
- 4 contract tests + 2 snapshots updated to match corrected spindle ranges
- Clamp-test inputs bumped so each clamp branch is still exercised (Laguna max: 25000→24000; Laguna min: 5000→6000)

### gcode-safety reference docs
The skill's reference files for Laguna and Carvera 3-axis still encoded the **old wrong spindle ranges**. Updated to match the corrected profiles + CLAUDE.md so future cycles don't read out-of-spec values from the safety docs.

---

## UI deep-dive + broken-path fix wave (2026-06-01 update)

### Broken paths closed (8 / 8)
| # | Fix | File |
| --- | --- | --- |
| 1 | IPC handler registration race — `register*Ipc` now runs BEFORE `createWindow()` inside `app.whenReady()`, with a comment-block documenting the ordering invariant | `src/main/index.ts` |
| 2 | NavRail Help button now wired — `onHelp: () => void` prop on NavRail; ShopApp passes the same handler F1 already uses | `src/renderer/src/NavRail.tsx`, `src/renderer/src/ShopApp.tsx` |
| 3 | NavRail 1–6 keyboard shortcuts wired — bare digit keys jump to Jobs / Tools / Workshop / My Shop / Library / Settings; gated on `isTypableKeyboardTarget` and "no modal open" | `src/renderer/src/ShopApp.tsx`, `src/shared/app-keyboard-shortcuts.ts` |
| 4 | ManufactureOperationList empty-filter state — "No operations match this filter" with a "Clear filter" button when a filter excludes everything | `src/renderer/manufacture/ManufactureOperationList.tsx` |
| 5 | ManufactureWorkspace Plan-tab no-setup banner — prominent inline banner with `Add Setup` CTA when `setupCount === 0` (replaces the buried sidebar message that users were missing) | `src/renderer/manufacture/ManufactureNoSetupBanner.tsx` (NEW), `src/renderer/manufacture/ManufactureWorkspace.tsx` |
| 6 | FirstLaunchWizard Escape handler — closes the WCAG 2.1.2 focus-trap-without-escape violation; gated on `!busy` so an in-flight IPC isn't dropped | `src/renderer/src/FirstLaunchWizard.tsx` |
| 7 | NavRail drawer-state desync — `effectiveActive` derivation keeps the rail's visual active state in sync with whichever drawer is open | `src/renderer/src/ShopApp.tsx` |
| 8 | Dialog file paths now null-byte-checked — `dialog:openFile / openFiles / saveFile` apply the same `\0`-include guard the other IPC paths use; rejected paths return the existing `null` / `[]` sentinel instead of throwing | `src/main/ipc-core.ts` |

### Design system hardening
- New shared **`EmptyState` component** (`src/renderer/src/EmptyState.tsx`) — readonly props (icon / title / body / cta), `data-testid` for pin tests, no inline styles, CTA reuses existing `.btn` primitives
- Empty-state BEM CSS (`.empty-state__icon`, `__title`, `__body`, `__cta`) added in `components.css` with design-token-only values
- Applied EmptyState to **3 high-traffic panels**: WorkshopDashboard (no jobs → "Create project" CTA), LagunaNestingPanel (no result → "Run nesting" CTA), SliceManufacturePanel (K2 connected, no slice → "Ready to slice")
- **Focus-visible rings** added to all interactive elements that were missing them: form inputs, tab buttons, plate tabs, WCS tabs, stock-material chips, filament-picker chips, etc.
- **Token consolidation**: replaced ~25 hard-coded pixel/color values in `components.css` with design tokens (`var(--sp-*)`, `var(--text-*)`, `var(--err)`/`var(--err-dim)`, etc.)

### Test deltas
| Surface | Tests added |
| --- | --- |
| EmptyState component | 10 |
| WorkshopDashboard (EmptyState wiring) | 4 |
| LagunaNestingPanel (EmptyState) | 4 |
| SliceManufacturePanel (EmptyState) | 5 |
| ManufactureOperationList (empty branches) | 5 |
| FirstLaunchWizard (Escape handler) | 5 |
| ManufactureNoSetupBanner | 5 |
| ipc-core null-byte | 6 |
| **Total** | **+44** (13,611 → 13,655) |

### Keyboard shortcuts (new in this wave)
- `1` → Jobs section
- `2` → Tools section
- `3` → Workshop dashboard
- `4` → My Shop quick-select
- `5` → Library drawer
- `6` → Settings drawer
- `F1` → Help panel (was orphaned in NavRail; now wired)
- `Escape` (in FirstLaunchWizard) → skip the wizard + persist `hasCompletedOnboarding`

All number-key shortcuts are gated on:
- focus NOT in a typable element (input/textarea/select/contenteditable)
- no modifier keys held (Ctrl/Meta/Shift/Alt)
- no modal / overlay open (command palette, shortcuts dialog, help panel, onboarding, first-launch wizard, gcode viewer, confirm dialogs)

---

## What you need to bring to each first real run

### Creality K2 Plus (FDM)
1. **Bundle OrcaSlicer**: `pwsh ./scripts/bundle-orca-slicer.ps1` (one-time)
2. **Power up the K2 Plus**, note its IP from the touchscreen Settings or your router DHCP table
3. **Set the Moonraker URL** in Settings → `http://<ip>:7125`
4. **Click Probe** in Settings — confirm the connection lights green, nozzle/bed temps appear
5. **Slice a small test STL** through the OrcaSlicer flow (the new `slice:orca` IPC handler routes through `runOrcaSlice`)
6. **Send to K2 Plus** via the WorkshopDashboard or the Manufacture panel's "Send to K2 Plus" button
7. Open Fluidd at `http://<ip>:7125` to watch the print progress

If anything fails: `docs/SMOKE-K2-MOONRAKER.md` covers the failure modes.

### Laguna Swift 5x10 (CNC router)
1. **Confirm the Laguna machine profile is active** (look for "Laguna Swift 5x10" in the brand-bar machine selector)
2. **Generate G-code** through CAM → Manufacture → Post with `vcarve_mach3.hbs` or `cnc_generic_mm.hbs`
3. **Save to a USB stick** (filename within RichAuto's depth limits, format `.nc` / `.mmg` / `.prg`)
4. **Insert the USB into the RichAuto pendant**, select the file
5. **Manual spindle warm-up** (M3 S6000 → ramp at the pendant before pressing Cycle Start)
6. **First cut at 1% feed override** to verify the WCS and tool length
7. **Dust collection ON before spindle on** — the Laguna post emits M7/M9 only if `dustCollection` flag is set on the job

Full runbook: `docs/SMOKE-LAGUNA-RICHAUTO.md`.

### Makera Carvera (3-axis or 4-axis)
1. **Install carvera-cli** (`pip install carvera-cli` in a Python virtualenv)
2. **Verify** with `carvera-cli --version`
3. **Configure** in Settings → External tool paths → `carveraCliPath` = absolute path to the exe
4. **Generate G-code** through CAM → Manufacture → Post with `carvera_3axis.hbs` or `carvera_4axis.hbs`
5. **Click Send to Carvera** in the WorkshopDashboard or the Manufacture row
6. **Watch for the success toast** — file is now on the Carvera SD card

For 4-axis jobs additionally:
- Read `docs/CAM_4TH_AXIS_REFERENCE.md` Part 1.5 (Physical Setup) BEFORE running
- Measure the chuck-face X-offset and dial-indicate chuck wobble < 0.05 mm
- Verify Y=0 alignment (180° A-axis sweep with dial indicator)
- Air-cut first, spindle off, full A-axis rotation

Full runbooks: `docs/SMOKE-CARVERA-CLI.md` + `docs/CAM_4TH_AXIS_REFERENCE.md`.

---

## Known limitations on first runs

These are explicit and documented; not bugs.

### K2 Plus
- **OrcaSlicer thumbnails not emitted in headless CLI** — the K2 G-code header validator soft-warns rather than fails when the `; thumbnail begin` block is absent. Verified in `src/main/slicer/orca-wrapper.e2e.test.ts`.
- **CFS multi-material is v1** — single filament per plate, no per-region color assignment yet.
- **OrcaSlicer binary must be bundled before first slice** — `runOrcaSlice` throws `orca_unavailable` with a clear hint until `pwsh ./scripts/bundle-orca-slicer.ps1` runs.

### Laguna Swift 5x10
- **No in-app upload helper** — USB is the only file transfer path (RichAuto pendant doesn't expose a network API in the bundled controller config)
- **No automatic dust-collection control** — the M7/M9 M-codes are emitted but require external wiring to a relay if your dust collector isn't already pendant-controlled
- **Vacuum zones (6-zone) are controlled from the pendant, NOT G-code** — `laguna-vacuum-postlude` emits opt-in M64/M65 only if explicitly enabled per-job
- **ATC is unsupported** — Laguna Swift is single-tool; the M6 path is intentionally not wired

### Makera Carvera (3-axis)
- **Minimum spindle RPM is now 13,000** — sub-13k jobs clamp upward with a warning. If your existing CAM workflow used 6,000–10,000 RPM, those jobs will now clamp; this is intentional (per spindle damage spec).
- **M30 in the footer would delete the file from the SD card on community firmware** — the post hardcodes M2 instead. Do not hand-edit M2 → M30.
- **Manual tool change opt-out** — `manualToolChange: true` per-op disables M6+G43; useful for diagnostic / single-tool / verification runs

### Makera Carvera (4-axis rotary)
- **Simultaneous 4-axis is opt-in and UNVERIFIED** — `enableSimultaneous4Axis: true` per-op emits a prominent warning header at the top of the G-code. Default is index-then-cut. Do not ship simultaneous to a real workpiece without an air-cut.
- **No schema enforcement of Y=0 or X-offset yet** — the post hardcodes `G0 Y0` and the engine enforces chuck-span pre-emission, but a misconfigured machine profile could still emit unsafe coordinates. Operator pre-cut sanity table in `docs/CAM_4TH_AXIS_REFERENCE.md` Part 1.5 is the operator-side safety net.

---

## Deferred items (do NOT block real-world testing)

Eight items remain as design-effort cycles. They are all defense-in-depth, not blocking:

### 1. Vitest 3.x → 4.x upgrade (rank 4, CVSS 9.8)
- **Severity**: critical (security)
- **Scope**: dev-dependency only; does NOT affect the shipping app or G-code emission
- **Blocker**: the 3.x → 4.x jump is a breaking change; needs a dedicated cycle to fix any vitest 4 API breakages
- **Decision needed**: schedule a `test-coverage` or `docs-and-dx` cycle for this upgrade

### 2. Carvera 4-axis Y=0 and X-offset schema constraints (rank 13)
- **Severity**: medium
- **Scope**: Add `yAxisMustBeZero: true` + `rotaryHeadstockXOffsetMm` to `machineProfileSchema`; extend `src/main/cam-axis4/validation.ts` to enforce these pre-generation
- **Today's safety net**: the hardcoded `G0 Y0` in `carvera_4axis.hbs:73` and the engine's chuck-span validator catch most cases; the operator pre-cut sanity table catches the rest
- **Decision needed**: cam-engine cycle to design + land the validator changes

### 3. Rename `cnc_4axis_grbl.hbs` → `carvera_4axis_grbl.hbs` (rank 16) — **LANDED**
- **Severity**: medium (clarity) — closed in the pre-launch punch-list closeout
- **Scope landed**: file renamed via `git mv`; `postTemplateMap` in `src/main/machine-cps-import.ts` repointed (all six 4-axis dialect entries); `COMMON_POST_TEMPLATE_FILENAMES` hint list updated; all relevant `src/main/post-process-*.test.ts` + `src/shared/machine-post-template-hints*.test.ts` pins updated; header comment block added to the renamed file documenting the April-2026 CPS fallback collapse and the rationale for the "carvera_" prefix
- **What did NOT change**: the schema dialect enum (still `grbl_4axis`); the legacy dialect-string `'cnc_4axis_grbl'` used in `src/main/cam-axis4/validation.ts` (it's a dialect identifier, not a filename); G-code emission is byte-for-byte identical (same template content); pinned snapshot keys updated to the new describe-block label, snapshot bodies unchanged

### Optional follow-up (not in punch list but worth filing)
**CPS-import fallback warning** — when a user imports a 5-axis Fanuc/Siemens CPS file, it silently falls back to the 3-axis generic post. A toast warning ("5-axis dialect detected → 3-axis generic fallback; 5-axis features will not be in the output") would close the UX gap. Not a safety issue (the UNVERIFIED header is sufficient) but worth a future ui-polish or cam-engine cycle.

### Big-design UX moves identified by the 2026-06-01 deep-dive — **4 OF 5 LANDED**

4. ✅ **Workflow-stage tabs above the viewport** — LANDED. `WorkflowStageTabs` in `ManufactureWorkspace.tsx` shows Prepare/Preview/Device for FDM (K2 Plus) and Setup/Toolpaths/Simulate/Send for CNC (Laguna / Carvera). Roving tabindex, arrow-key/Home/End nav. Panel-content swap per stage is a follow-up cycle (V1.5) — for now the existing panels render unchanged regardless of stage, but the chrome is in place.
5. ⏸️ **Right-side profile stack with Recommended/Pro modes + Send-to-machine primary action** — DEFERRED. Touches `ManufactureWorkspace.tsx` + `ManufactureAuxPanels.tsx` + `manufacture.css` in ways that need structural rethinking of the properties panel. Best done as its own cycle.
6. ✅ **Setup-rooted operation tree with status icons** — LANDED. `ManufactureOperationList.tsx` restructured as a tree: each Setup is a parent node with op-count badge + collapse chevron; ops nest underneath with left-edge status icons (stale/error/done/running/idle). Synthetic "(Unassigned)" group for ops with no setup.
7. ✅ **Locked global status strip** — LANDED. New `AppHeader.tsx` renders fixed-top above the brand-bar: StatusDot + state label (left), machine ID in mono (center), conditional E-stop slot (right). State derived from the same 5-second moonrakerStatus poll the WorkshopDashboard uses for K2; from latest job for Laguna/Carvera. E-stop wiring is V2 (`onEstop` prop currently unwired).
8. ✅ **Multi-plate thumbnail strip** — LANDED. `PlateTabs.tsx` now renders 120×80 thumbnail tiles per plate with preview placeholder + name + status pill (idle/slicing/done/error) + top-right × close. Dashed "+ Add plate" tile at the end. Adjacent split Slice button (Slice this plate / Slice all).

PLUS: ✅ **Brand-bar Design pill** — LANDED. New `.shop-brand-bar__design-pill` button next to the env-switcher triad opens the Design overlay. Discoverability for the CAD workspace beyond Ctrl+Shift+D.

PLUS: ✅ **Vitest 3.x → 4.1.8 upgrade** — LANDED. CVE GHSA-5xrq-8626-4rwp (CVSS 9.8 arbitrary file read/execute) **CLOSED**. Test suite still green: 13,844 passing.

PLUS: ✅ **`npm run test:python` script re-wired** — LANDED. Sidecar pytest suite runs again (30 cases pass, 7 expected-skipped). Lost during the 2026-05-27 pivot's package.json cleanup.

---

## CAD Design workspace MVP (2026-06-01 update)

### What landed
The app is no longer CAM-only. A full parametric-CAD design surface is wired in, sharing the same Electron shell, sidecar, viewport, project file, and design tokens.

**Backend** (Python sidecar + TS IPC + schema)
- `engines/sidecar/cad_handlers.py` — 3 new RPC methods:
  - **`cad.execute_script`** — runs user CadQuery code via `cqgi.parse(...).build(...)` with a BANNED_TOKENS pre-scan (rejects `import os/sys/subprocess/socket/shutil`, `__import__`, `open(`, `eval(`, `exec(`, etc.). Tessellates result, returns mesh + faceCount + log + error envelope.
  - **`cad.export`** — exports the last script result to STEP / STL / DXF with path-safety validation. Used by the Send-to-CAM handoff (STL at 0.1 mm tolerance).
  - **`cad.list_operations`** — pure AST parse (no exec) of the script for the read-only feature tree; safe to run on every keystroke (debounced 300 ms in the renderer).
- `engines/cad/cadquery_script.py` — script execution core (tessellation, handle registry, exporters)
- `src/main/ipc-cad.ts` — TS IPC layer (`cad:execute`, `cad:export`, `cad:listOperations`)
- `src/preload/index.ts` + `src/renderer/src/shop-types.ts` — `window.fab.cad.{execute, export, listOperations}` typed bridge
- `src/shared/sidecar-protocol.ts` — wire-type pairing for the 3 new methods
- `src/shared/project-schema.ts` — new optional `designModels: DesignModel[]` field with `.default([])` for backward compatibility. Schema additions: `designTessellationSchema`, `designModelSchema`. STL caches live at `<projectDir>/assets/designs/<designId>/tessellation_<toleranceMm>mm.stl`.

**Frontend** (React components + integration)
- `src/renderer/design/CadQueryEditor.tsx` — code editor pane (controlled `<textarea>` with mono font, Run button, busy state, debounced `list_operations` calls)
- `src/renderer/design/FeatureTree.tsx` — read-only parametric history with Parameters + Operations sections. Uses `EmptyState` when empty.
- `src/renderer/design/DesignWorkspace.tsx` — fullscreen overlay composing editor + viewport + tree + Send-to-CAM button
- `src/renderer/src/ShopApp.tsx` — Design overlay state (`designOpen`), Ctrl+Shift+D shortcut, Escape-to-close, command palette entry ("Open Design workspace"), Send-to-CAM unification handler
- `src/shared/app-keyboard-shortcuts.ts` — new Ctrl+Shift+D entry in the Navigation rail group

**Sample CAD scripts** (shipped by the wizard)
- `resources/samples/cad/bracket.cq.py` — L-bracket (rect + extrude + 2 holes + 2 fillets) for Laguna/Carvera 3-axis
- `resources/samples/cad/sign.cq.py` — flat plate + extruded text for Laguna v-carve
- `resources/samples/cad/cylinder.cq.py` — 30mm OD × 80mm length cylinder + helical groove for Carvera 4-axis

**First-launch wizard** — 4th option "Start a parametric design" alongside the 3 machine envs. User picks target machine; wizard creates project, preloads the matching CadQuery sample into `designModels[0]`, opens the Design overlay with that script loaded.

**Send-to-CAM handoff** (the unifying button)
1. DesignWorkspace's "Send to CAM" calls `cad.export({format: 'stl', outPath: <project assets dir>})`
2. On success: switches back to the active machine env (or prompts if none set)
3. Auto-imports the produced STL into the active project's first plate via the existing STL import flow
4. Shows success toast: "Design exported and loaded into the CAM workspace"

### Test deltas
| Surface | New tests |
| --- | --- |
| Sidecar `cad.execute_script` / `cad.export` / `cad.list_operations` | 30 (pytest) |
| `ipc-cad` TS handler + preload bridge | 41 |
| `project-schema` + `project-store` (designModels round-trip) | 18 |
| `CadQueryEditor` + `FeatureTree` render pins | 29 |
| `DesignWorkspace` + CAD-to-CAM handoff pin | 23 |
| FirstLaunchWizard 4-option layout + sample on-disk pin | 17 |
| **Total** | **+158** (13,655 → 13,787 vitest) |

### User progression (now genuinely simple)
```
First launch → wizard offers 4 options:
   ┌─ Pick a machine
   │     └─ Project opens in CAM env, ready to import STL
   │
   └─ Start a parametric design + pick a target machine
         └─ Project opens in Design overlay with starter CadQuery script
            ├─ Edit script → Run → 3D result + feature tree
            └─ Send to CAM → STL exported + machine env activated +
               STL auto-loaded into the first plate → generate G-code
```

The .wtcam project file holds both CAD designs and CAM jobs — single source of truth across the design→manufacture flow.

### Known limitations on the CAD MVP
- **Script-first only** — no visual sketch editor yet (Monaco code highlighting also deferred). The textarea uses the mono font.
- **Read-only feature tree** — editing parameters in the tree (instead of in the script) is V1 scope.
- **No selection system** — can't pick faces/edges from the 3D viewport to apply ops to. Script-only addressing for now.
- **No constraint solver** — for V1, add `planegcs` (the same solver FreeCAD uses).
- **No assemblies / drawings** — V2 scope.
- **Design discoverability**: Ctrl+Shift+D + command palette + first-launch wizard. No NavRail entry (intentional — Design is an overlay, not a workspace section).

### Architectural choice worth knowing
The Design workspace renders as a **fullscreen overlay** rather than as a registered 4th env in `environments/registry.ts`. Rationale:
- Preserves CAM env state across CAD work — when you click Send-to-CAM, you return to exactly the CAM workspace state you left.
- Matches Mainsail/Fluidd's pattern of treating gcode preview / console as overlays, not envs.
- Simpler — no env-switcher 4th button, no per-env state replication.
- Trade-off: less discoverable than a brand-bar env button would be. If needed later, adding a brand-bar Design pill is a small refactor.

---

## Security note

Latest `npm audit` reports **17 vulnerabilities** (2 critical, 11 high, 3 moderate, 1 low). The 2 critical advisories are GHSA-5xrq-8626-4rwp (vitest arbitrary file read/execute) — covered by deferred item 1 above. The remaining 15 are transitive dev-dependencies that don't affect the shipping Electron app, but should be triaged in a future security-focused cycle.

GitHub's Dependabot will continue to surface these on every push. Suppressing them is not the answer; the vitest upgrade resolves the criticals.

---

## What to do if something goes wrong

1. **First, look in the run log** — most failures have explicit operator-visible warning lines in the emitted G-code (UNVERIFIED, "clamped to", "below machine minimum", etc.)
2. **Check the relevant SMOKE-* doc** — each runbook has a Troubleshooting section
3. **Run the gcode-safety skill** on the affected post template before re-running on the machine
4. **Open an issue** if a real failure surfaces — include the machine profile JSON, the input toolpath, and the emitted G-code

---

## Confidence summary

| Surface | Confidence | Why |
| --- | --- | --- |
| K2 Plus FDM pipeline (slice → push) | HIGH | E2E test against real bundled binary green; Cycle 8f7a54d CFS + PLR + adaptive-probe header validators |
| Laguna Swift G-code | HIGH | Post unchanged, profile corrected, contract pins green at the new ranges |
| Carvera 3-axis G-code | HIGH | M2 vs M30 guard pinned; ATC orchestration pinned; spindle floor now safe |
| Carvera 4-axis G-code | MEDIUM-HIGH | Engine + chuck-span validator + operator sanity table all in place; schema-level enforcement deferred |
| First-time-user docs | HIGH | All four SMOKE-* runbooks present + cross-linked from README |
| CSS coverage | HIGH | Workshop dashboard + calibration + nesting + plate-tabs + settings-probe all styled |
| Security (dev-deps) | MEDIUM | Two critical CVEs in vitest deferred to a dedicated cycle |

Real-world testing can begin.
