# WorkTrack3D Python sidecar dependencies
# Python 3.9-3.11 required (OpenCAMLib wheel compatibility range).
#
# Install:
#   pip install -r engines/requirements.txt

# ── CAD kernel ────────────────────────────────────────────────────────
# cadquery: parametric B-rep modeling on OpenCascade (Apache 2)
cadquery>=2.4,<3

# planegcs: Python binding to FreeCAD's 2D geometric constraint solver
# (LGPL). Powers the CAD V1 Sketcher's planegcs-backed constraint solve.
# Wheel availability is platform-specific (cp39–cp311 + manylinux/win/macOS);
# the sidecar gracefully degrades to ``planegcs_not_installed`` when absent.
planegcs>=0.6.0,<1

# ── CAM toolpath generation ───────────────────────────────────────────
# opencamlib: drop-cutter / push-cutter / waterline (LGPL)
opencamlib>=2023.1

# ── Mesh + numerics ───────────────────────────────────────────────────
# numpy: vectorized STL loading, drop-cutter heightfields, surface analysis
numpy>=1.21,<3

# trimesh: OBJ/PLY/GLB/3MF/… → STL (engines/mesh/mesh_to_stl.py)
trimesh>=4,<5

# ── Optional (enhanced developer experience) ──────────────────────────
# structlog: structured JSON log output; engine falls back to stdlib logging
structlog>=23.1,<26
